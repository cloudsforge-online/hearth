{
  "name": "hearth-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Hearth desktop miner — mine EMBER on macOS, Windows or Linux (Tauri)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "runtime": "node scripts/fetch-node.mjs",
    "prebuild": "node scripts/fetch-node.mjs",
    "build": "tauri build",
    "icons": "tauri icon ../web/assets/logo.svg",
    "test": "node test/wiring.js && node test/engine.js && cargo test --manifest-path src-tauri/Cargo.toml",
    "test:wiring": "node test/wiring.js",
    "test:engine": "node test/engine.js",
    "selftest": "src-tauri/target/release/hearth-desktop --selftest"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
